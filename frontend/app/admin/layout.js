export const metadata = {
  title: 'FLOLIA 管理画面',
  description: 'FLOLIA Kickboxing Studio 予約管理',
  robots: {
    index: false,
    follow: false,
    googleBot: {
      index: false,
      follow: false,
    },
  },
}

export default function AdminLayout({ children }) {
  return children
}
